apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kafka
  labels:
    app: {{ .Release.Name }}-kafka
spec:
  selector:
    app: {{ .Release.Name }}-kafka
  ports:
  - name: plaintext
    port: 9092
    targetPort: 9092
  type: ClusterIP
